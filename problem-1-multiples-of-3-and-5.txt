function multiplesOf3and5(number) {
  // Good luck!
  var sum = 0;
  var i = 0;

  while(i < number)
  {
    i%3 == 0 ? sum+=i : i%5 == 0 ? sum+=i : null; 

    i++;
  }

  return sum;
}

multiplesOf3and5(1000);
